<%= form_for(@article) do |article| %>
	<% if @article.errors.any? %> 
	<div id="error_explanation">
		<h2><%= pluralize(@article.errors.count, "error") %> prohibited this post from being saved:
		</h2>

		<ul>
			<% @article.errors.full_messages.each do |message| %>
			<li><%= message %></li>
			<% end %>
		</ul>
	</div>
	<% end %>

	<div class="field">
		<%= article.label :name %><br>
		<%= article.text_field :name %>
	</div>
	<div class="field">
		<%= article.label :body %><br>
		<%= article.text_area :body %>
	</div>
	<div class="actions">
		<%= article.submit %>
	</div>
	<% end %>
